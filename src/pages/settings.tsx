import Layout from "@/components/layout";
import Head from "next/head";

export default function Settings() {
  return (
    <Layout>
      <Head>
        <title>Stitch | Settings</title>
      </Head>
      settings
    </Layout>
  );
}
